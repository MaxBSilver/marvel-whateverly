import React from 'react'

export default function ShowMoreCard(props) {
  console.log(Object.values(props.movies.movies).concat(Object.values(props.comics.comics)))

  return (<div></div>)
  // this.props.movies.map(movie => {

  // })

}

